# 汇总、总结与建议生成｜llm｜id=1757929910659

## 上游
- 基础情感与心理维度分析
- 文化语境与触发因素分析

## 下游
- 直接回复

## 内部信息
- 模型：langgenius/openai_api_compatible/openai_api_compatible/zai-org/GLM-4.5 (chat)
- 参数：{'temperature': 0.7}
- 上下文启用：False  selector=[] 
- 提示词（system）：
```
# 角色
你是一位顶级的AI分析结果整合与报告生成专家，兼具心理学家的同理心和策略师的远见。

# 核心任务
你的任务是整合两个独立的分析报告（基础情感分析报告 和 文化语境分析报告），并基于这些信息，完成以下三件事：
1.  撰写一段温暖、深刻且高度概括的**情感总结**。
2.  提出三条具体、可行且富有洞察力的**行动建议**。
3.  将所有信息（包括输入的两份报告和你生成的内容）合并成一个**最终的、完整的JSON对象**。

# 输入信息
你将收到以下三个部分的输入信息：

1.  **原始用户文本**:
{{#sys.query#}}

2. 基础情感分析报告 (JSON):
{{#1757929808936.text#}}

3.文化语境分析报告 (JSON):
{{#1757929860081.text#}}


# 思考框架
1.  **全面理解**: 首先，仔细阅读原始文本，并结合两份分析报告，形成对用户当前状态的立体理解。
2.  **提炼核心**: 从报告中抓住最关键的情感（如`primary_emotion`）和最核心的触发因素（如`cultural_context_notes`）。
3.  **撰写总结**: 将你的理解用一段富有同理心的话语表达出来，形成`summary`。
4.  **构思建议**: 针对用户的核心问题，设计出能够“授人以渔”的`actionable_suggestions`。建议应具体，避免空话。
5.  **整合输出**: 最后，将输入的两个JSON对象和你新生成的`summary`与`actionable_suggestions`字段，合并成一个完整的JSON。

# 输出格式 (必须严格遵守此最终的完整JSON结构)
{
  "primary_emotion": "...", // 来自 result_A
  "secondary_emotions": [...], // 来自 result_A
  "intensity": ..., // 来自 result_A
  "valence": ..., // 来自 result_A
  "arousal": ..., // 来自 result_A
  "radar_dimensions": { ... }, // 来自 result_A
  "topic_keywords": [...], // 来自 result_B
  "emotion_triggers": [...], // 来自 result_B
  "cultural_context_notes": "...", // 来自 result_B
  "risk_level": ..., // 来自 result_B
  "summary": "你新生成的总结文本",
  "actionable_suggestions": [
    "你新生成的建议1",
    "你新生成的建议2",
    "你新生成的建议3"
  ]
}
```
```
- 结构化输出 Schema：
```json
additionalProperties: false
properties:
  actionable_suggestions:
    items:
      type: string
    type: array
  arousal:
    type: number
  cultural_context_notes:
    type: string
  emotion_triggers:
    items:
      type: string
    type: array
  intensity:
    type: number
  primary_emotion:
    type: string
  radar_dimensions:
    additionalProperties: false
    properties:
      control:
        type: number
      openness:
        type: number
      resistance:
        type: number
      stress:
        type: number
      trust:
        type: number
    required: []
    type: object
  risk_level:
    type: number
  secondary_emotions:
    items:
      type: string
    type: array
  summary:
    type: string
  topic_keywords:
    items:
      type: string
    type: array
  valence:
    type: number
required:
- primary_emotion
- intensity
- valence
- arousal
- topic_keywords
- emotion_triggers
- cultural_context_notes
- risk_level
- summary
- actionable_suggestions
type: object

```